/**
 * Vercel API URL for the Musician Roster backend.
 *
 * Set this after you deploy the `website` folder on Vercel (see ROSTER_AUTOMATION.md),
 * OR set VITE_ROSTER_API_BASE in `.env.production` before `npm run deploy`.
 *
 * Example: "https://studio-website.vercel.app" (no trailing slash)
 */
export const ROSTER_API_BASE_FALLBACK = "https://studio-theta-gules.vercel.app";
